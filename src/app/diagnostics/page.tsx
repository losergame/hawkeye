import { SheetsDiagnostics } from "@/components/sheets/sheets-diagnostics";

export const metadata = { title: "Sheets Diagnostics | Hawkeye" };

export default function DiagnosticsPage() {
  return <SheetsDiagnostics />;
}
