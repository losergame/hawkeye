import { SignalPerformanceDashboard } from "@/components/signals/signal-performance-dashboard";

export const metadata = { title: "Signal Performance | Hawkeye" };

export default function SignalsPage() {
  return <SignalPerformanceDashboard />;
}
